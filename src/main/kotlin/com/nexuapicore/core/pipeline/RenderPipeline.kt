package com.nexuapicore.core.pipeline

import com.nexuapicore.core.module.NexusModule

object RenderPipeline {
    fun assemble(modules: List<NexusModule>) {
        // futura montagem dos estágios
    }
    fun executeFrame() {}
}
